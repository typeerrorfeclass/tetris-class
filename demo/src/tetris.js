const TILE_NUM_X = 10
const TILE_NUM_Y = 20

export default class Tetris {
  get el () {
    return this.el_
  }

  static createHTMLContent () {



  }

  constructor (sel) {
    const el = this.el_ = document.querySelector(sel)

    el.innerHTML = 
  }
}
